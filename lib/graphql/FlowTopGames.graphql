query FlowTopGames($first: Int, $after: Cursor) {
  games(first: $first, after: $after) {
    edges {
      cursor
      node {
        id
        displayName
        boxArtURL
      }
    }
    pageInfo {
      hasNextPage
    }
  }
}
